import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'TeLlevaApp_2.0',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
